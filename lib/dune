(library
 (public_name microasm)
 (name microasm)
 (modules base pprint checker lexer parser build trace interpreter solver)
 (libraries z3))

(menhir
 (modules parser))

(ocamllex lexer)

; (env
;  (dev
;   (flags
;    (:standard -w -a))))
